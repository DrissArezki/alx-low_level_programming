#include "main.h"
/**
 * _abs - Computes the absolute value of an integer
 * @a: the integer to use
 * Return: the absolute value of a
 */
int _abs(int a)
{
	if (a < 0)
	{
		return (-a);
	}
	return (a);
}
